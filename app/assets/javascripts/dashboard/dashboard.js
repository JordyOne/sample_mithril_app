var dashboard = {};
dashboard.controller = function () {
    dashboard.vm.init();
};

dashboard.vm = {};
dashboard.vm.init = function () {
    this.todo = todo;
};

dashboard.view = function () {
    var vm = dashboard.vm;
};
